import { Commands } from "../typings/typings.js";


const configCommand: Commands = {
    name: "config",
    description: "Yapılandırma Komutu",
    execute: (agent, message, ...args) => {
        
    },
}